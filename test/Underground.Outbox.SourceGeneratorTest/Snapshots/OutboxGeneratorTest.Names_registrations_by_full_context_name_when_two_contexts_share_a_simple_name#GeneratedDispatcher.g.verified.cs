//HintName: GeneratedDispatcher.g.cs
// <auto-generated/>
#nullable enable
#pragma warning disable CS1591 // Missing XML comment for publicly visible type or member

using System;
using System.Text.Json;

using Underground.Outbox.Data;
using Underground.Outbox.Domain.Dispatchers;
using Underground.Outbox.Exceptions;
using Microsoft.Extensions.DependencyInjection;

namespace Underground.Outbox.Domain;

/// <summary>Dispatches the messages of the Orders.AppDbContext inbox and outbox to the handlers bound to it.</summary>
internal sealed class Dispatcher_Orders_AppDbContext<TMessage> : IMessageDispatcher<global::Orders.AppDbContext, TMessage> where TMessage : class, IMessage
{
    public async Task ExecuteAsync(IServiceScope scope, TMessage message, CancellationToken cancellationToken)
    {
        var metadata = new MessageMetadata(message.EventId, message.GroupKey, message.RetryCount);
        var serviceProvider = scope.ServiceProvider;
        if (typeof(TMessage) == typeof(InboxMessage))
        {
            throw new ParsingException($"No handler configured for message type {message.Type} of message: {message.Id}");
        }

        if (typeof(TMessage) == typeof(OutboxMessage))
        {
            if (string.Equals(message.Type, typeof(global::Orders.OrderPlaced).FullName, StringComparison.Ordinal))
            {
                var fullEvent = JsonSerializer.Deserialize<global::Orders.OrderPlaced>(message.Data) ?? throw new ParsingException($"Cannot parse event body {message.Data} of message: {message.Id}");
                var handler = serviceProvider.GetRequiredKeyedService<IOutboxMessageHandler<global::Orders.OrderPlaced>>(typeof(global::Orders.AppDbContext));
                try
                {
                    await handler.HandleAsync(fullEvent, metadata, cancellationToken);
                    return;
                }
                catch (Exception ex) when (ex is not OperationCanceledException)
                {
                    throw new MessageHandlerException(
                        handler.GetType(),
                        typeof(global::Orders.OrderPlaced),
                        $"Error processing message {message.Id} with handler {handler.GetType().Name}",
                        ex
                    );
                }
            }

            throw new ParsingException($"No handler configured for message type {message.Type} of message: {message.Id}");
        }

        throw new ParsingException($"Unsupported dispatcher message type {typeof(TMessage).FullName}");
    }
}

/// <summary>Dispatches the messages of the Orders.Billing.AppDbContext inbox and outbox to the handlers bound to it.</summary>
internal sealed class Dispatcher_Orders_Billing_AppDbContext<TMessage> : IMessageDispatcher<global::Orders.Billing.AppDbContext, TMessage> where TMessage : class, IMessage
{
    public async Task ExecuteAsync(IServiceScope scope, TMessage message, CancellationToken cancellationToken)
    {
        var metadata = new MessageMetadata(message.EventId, message.GroupKey, message.RetryCount);
        var serviceProvider = scope.ServiceProvider;
        if (typeof(TMessage) == typeof(InboxMessage))
        {
            throw new ParsingException($"No handler configured for message type {message.Type} of message: {message.Id}");
        }

        if (typeof(TMessage) == typeof(OutboxMessage))
        {
            if (string.Equals(message.Type, typeof(global::Orders.Billing.InvoiceRaised).FullName, StringComparison.Ordinal))
            {
                var fullEvent = JsonSerializer.Deserialize<global::Orders.Billing.InvoiceRaised>(message.Data) ?? throw new ParsingException($"Cannot parse event body {message.Data} of message: {message.Id}");
                var handler = serviceProvider.GetRequiredKeyedService<IOutboxMessageHandler<global::Orders.Billing.InvoiceRaised>>(typeof(global::Orders.Billing.AppDbContext));
                try
                {
                    await handler.HandleAsync(fullEvent, metadata, cancellationToken);
                    return;
                }
                catch (Exception ex) when (ex is not OperationCanceledException)
                {
                    throw new MessageHandlerException(
                        handler.GetType(),
                        typeof(global::Orders.Billing.InvoiceRaised),
                        $"Error processing message {message.Id} with handler {handler.GetType().Name}",
                        ex
                    );
                }
            }

            throw new ParsingException($"No handler configured for message type {message.Type} of message: {message.Id}");
        }

        throw new ParsingException($"Unsupported dispatcher message type {typeof(TMessage).FullName}");
    }
}
#pragma warning restore CS1591
